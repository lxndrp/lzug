import { Component } from '@angular/core';

/** Empty product hook replaced only while building the separate demo assembly. */
@Component({
  selector: 'app-runtime-notice',
  template: '',
})
export class RuntimeNoticeComponent {}
